// scrMatchListError(error,httpStatus)

var error = argument0;
var httpStatus = argument1;
// Show a message to indicate that the load failed
show_message_async("Failed to load matches");
// We aren't loading any more
loading = false;
